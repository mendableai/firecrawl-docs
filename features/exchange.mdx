---
title: "Exchange"
description: "Call approved data providers through one Firecrawl contract"
og:title: "Exchange | Firecrawl"
og:description: "Call approved data providers through one Firecrawl contract"
icon: "arrow-right-arrow-left"
---

<iframe
  title="An agent answering a question with one Exchange request"
  sandbox="allow-scripts"
  style={{ width: "100%", height: "430px", border: "0", colorScheme: "dark" }}
  srcDoc={`<!doctype html><html><head><meta charset="utf-8"><style>
*{box-sizing:border-box}
html,body{height:100%}
body{margin:0;padding:12px 0 10px;display:flex;flex-direction:column;
  font:13px/1.85 ui-monospace,SFMono-Regular,Menlo,Consolas,"Liberation Mono",monospace;
  background:#26313B;color:#E4E9ED;border-radius:10px;overflow:hidden}
#tabs{display:flex;gap:6px;justify-content:center;margin:0 14px 10px;flex:0 0 auto}
button{font:inherit;font-size:11px;cursor:pointer;padding:3px 10px;border-radius:999px;
  border:1px solid #3C4854;background:transparent;color:#94A1AD;transition:.15s}
button:hover{color:#E4E9ED;border-color:#55636F}
button.on{background:#FA5D19;border-color:#FA5D19;color:#fff}
#b{flex:1 1 auto;min-height:0;overflow:hidden;display:flex;flex-direction:column;
  justify-content:flex-end;padding:0 14px;mask-image:linear-gradient(transparent,#000 8%)}
.row,.sp,.user{flex:0 0 auto}
.row{white-space:pre-wrap}
.user{background:#323E49;margin:0 -14px;padding:1px 14px;white-space:pre-wrap;color:#E4E9ED}
.chev{color:#7C8794}
.dot{color:#E4E9ED}
.dotg{color:#7C8794}
.g{color:#94A1AD}
.d{color:#6E7A86}
.ok{color:#63C89B}
.acc{color:#FA5D19}
.ind{padding-left:20px}
.sp{height:10px}
.cur{display:inline-block;width:7px;height:14px;background:#FA5D19;vertical-align:-2px;
  animation:blink 1.1s steps(2,start) infinite}
@keyframes blink{50%{opacity:0}}
#status{flex:0 0 auto;padding:6px 14px 0;color:#FA5D19;min-height:22px}
</style></head><body>
<div id="tabs"></div>
<div id="b"></div>
<div id="status"></div>
<script>
var SCENES = [
  { id:'Firmographic',
    prompt:'a lead just came in from someone@firecrawl.dev',
    think:'I need the company record, who works there, and recent coverage.',
    findQ:'company funding and people by title',
    found:[['crunchbase/company-search','25 credits per call'],
           ['apollo/people-search','25 credits per call']],
    callQ:'firecrawl company and its engineering leadership',
    calls:2,
    rows:[['crunchbase/company-search','Firecrawl · founded 2024 · seed'],
          ['apollo/people-search','4 people · founders and eng leads'],
          ['news','Firecrawl ships Exchange for agents']],
    credits:'51 credits · 25 + 25 providers, 1 web',
    done:'Real company, seed stage, four people worth knowing, shipping this month.' },
  { id:'Finance',
    prompt:'who is funding agent infrastructure right now',
    think:'Funding events are structured records, not something to scrape.',
    findQ:'funding rounds and acquisitions',
    found:[['crunchbase/funding-round-search','50 credits per call'],
           ['crunchbase/acquisition-search','25 credits per call']],
    callQ:'agent infrastructure rounds this quarter',
    calls:2,
    rows:[['crunchbase/funding-round-search','14 rounds · $2.3B raised'],
          ['crunchbase/acquisition-search','3 acquisitions · buyers attached']],
    credits:'75 credits · 50 + 25 providers, no web query',
    done:'Amounts, dates, and investors, already structured. No parsing.' },
  { id:'News',
    prompt:'what is being written about agent infrastructure',
    think:'I want reporting with metadata, not a page scrape.',
    findQ:'long-form reporting and analysis articles',
    found:[['the-atlantic/article-search','10 credits per call']],
    callQ:'agent infrastructure coverage',
    calls:1,
    rows:[['the-atlantic/article-search','4 articles · 100-word previews'],
          ['news','12 results from the web']],
    credits:'11 credits · 10 provider, 1 web',
    done:'Long-form analysis and fresh coverage, in one response.' }
];

var SPIN = ['✻','✳','✶','✻'];
var WORDS = ['Flowing','Simmering','Percolating','Noodling'];
var host = document.getElementById('b');
var tabs = document.getElementById('tabs');
var statusEl = document.getElementById('status');
var run = 0, tokens = 0, t0 = 0, statusTimer = null, word = 'Flowing';

function wait(ms){ return new Promise(function(r){ setTimeout(r, ms); }); }
function el(cls){ var d = document.createElement('div'); d.className = (cls || 'row'); host.appendChild(d); trim(); return d; }
function gap(){ var d = document.createElement('div'); d.className = 'sp'; host.appendChild(d); }
function pad(s, n){ while (s.length < n) s += ' '; return s; }
function fmt(n){ return n >= 1000 ? (n / 1000).toFixed(1) + 'k' : String(n); }

// The persistent status line, the way the CLI keeps time and tokens on screen.
function startStatus(){
  t0 = Date.now(); tokens = 0; var f = 0;
  clearInterval(statusTimer);
  statusTimer = setInterval(function(){
    var e = Math.round((Date.now() - t0) / 1000);
    var clock = e < 60 ? e + 's' : Math.floor(e / 60) + 'm ' + (e % 60) + 's';
    tokens += Math.round(Math.random() * 45);
    statusEl.innerHTML = '<span>' + SPIN[f++ % 4] + ' ' + word + '… </span><span class="d">(' +
      clock + ' · ↓ ' + fmt(tokens) + ' tokens · esc to interrupt)</span>';
  }, 120);
}
function stopStatus(){ clearInterval(statusTimer); statusEl.innerHTML = ''; }

async function stream(target, segs, cps, mine){
  for (var i = 0; i < segs.length; i++){
    var span = document.createElement('span');
    if (segs[i][1]) span.className = segs[i][1];
    target.appendChild(span);
    var txt = segs[i][0];
    for (var j = 0; j < txt.length; j++){
      if (run !== mine) return;
      span.textContent += txt[j];
      if (j % 2 === 0) await wait(cps);
    }
  }
}

// A tool call renders as "Calling…" and is rewritten in place to "Called".
function toolLine(name, times){
  var d = el('row');
  var suffix = times > 1 ? ' ' + times + ' times' : '';
  d.innerHTML = '<span class="dotg">●</span> <span class="g">Calling ' + name + suffix +
    '… </span><span class="d">(ctrl+o to expand)</span>';
  return { node: d, name: name, suffix: suffix };
}
function toolDone(t){
  t.node.innerHTML = '<span class="dot">●</span> <span>Called ' + t.name + t.suffix +
    '</span> <span class="d">(ctrl+o to expand)</span>';
}

// Old turns are kept so the panel reads as one continuing session rather than
// resetting to an empty box, but the transcript is trimmed so it cannot grow
// without bound.
function trim(){
  while (host.children.length > 42) host.removeChild(host.firstChild);
}

async function play(scene, mine){
  if (host.children.length) gap();
  word = WORDS[Math.floor(Math.random() * WORDS.length)];

  var u = el('user');
  u.innerHTML = '<span class="chev">〉</span> ';
  await stream(u, [[scene.prompt, '']], 14, mine);
  await wait(600); if (run !== mine) return;
  startStatus();

  gap();
  var a = el('row'); a.innerHTML = '<span class="dot">●</span> ';
  await stream(a, [[scene.think, '']], 10, mine);
  await wait(500); if (run !== mine) return;

  gap();
  var t1 = toolLine('firecrawl_exchange', 1);
  var q1 = el('row ind g');
  q1.innerHTML = '<span class="d">⎿</span>  ';
  await stream(q1, [['"' + scene.findQ + '"', 'd']], 7, mine);
  await wait(1600); if (run !== mine) return;
  toolDone(t1);
  q1.innerHTML = '<span class="d">⎿</span>  <span class="g">found ' + scene.found.length + ' ' +
    (scene.found.length === 1 ? 'capability' : 'capabilities') + ' · 0 credits</span>';
  var fw = 0;
  scene.found.forEach(function(r){ if (r[0].length > fw) fw = r[0].length; });
  fw += 2;
  for (var k = 0; k < scene.found.length; k++){
    var fr = el('row ind g'); fr.innerHTML = '   ';
    await stream(fr, [[pad(scene.found[k][0], fw), 'd'], [scene.found[k][1], 'd']], 5, mine);
    await wait(260); if (run !== mine) return;
  }
  await wait(700); if (run !== mine) return;

  gap();
  var t2 = toolLine('firecrawl_exchange', scene.calls);
  var q2 = el('row ind g');
  q2.innerHTML = '<span class="d">⎿</span>  ';
  await stream(q2, [['"' + scene.callQ + '"', 'd']], 7, mine);
  await wait(2200); if (run !== mine) return;
  toolDone(t2);

  var w = 0;
  scene.rows.forEach(function(r){ if (r[0].length > w) w = r[0].length; });
  w += 2;
  q2.innerHTML = '<span class="d">⎿</span>  ';
  await stream(q2, [[pad(scene.rows[0][0], w), 'g'], [scene.rows[0][1], 'g']], 5, mine);
  await wait(380); if (run !== mine) return;
  for (var i = 1; i < scene.rows.length; i++){
    var r = el('row ind g'); r.innerHTML = '   ';
    await stream(r, [[pad(scene.rows[i][0], w), 'g'], [scene.rows[i][1], 'g']], 5, mine);
    await wait(380); if (run !== mine) return;
  }
  var cr = el('row ind d'); cr.innerHTML = '   ';
  await stream(cr, [[scene.credits, 'd']], 5, mine);
  await wait(800); if (run !== mine) return;

  stopStatus();
  gap();
  var d = el('row'); d.innerHTML = '<span class="dot">●</span> ';
  await stream(d, [[scene.done, '']], 10, mine);
  var cur = document.createElement('span'); cur.className = 'cur'; d.appendChild(cur);
  await wait(2400);
}

async function loop(scene, mine){ while (run === mine){ await play(scene, mine); } }

function select(idx){
  run++; stopStatus(); host.innerHTML = '';
  var mine = run;
  [].forEach.call(tabs.children, function(btn, i){ btn.className = i === idx ? 'on' : ''; });
  loop(SCENES[idx], mine);
}

SCENES.forEach(function(sc, i){
  var btn = document.createElement('button');
  btn.textContent = sc.id;
  btn.onclick = function(){ select(i); };
  tabs.appendChild(btn);
});
select(0);
</script></body></html>`}
/>

A lead lands from someone at Firecrawl. Before anyone replies, your agent needs
three things the web will not give you cleanly:

1. Is the company real, funded, and how big?
2. Who works there and at what level?
3. What have they shipped in the last month?

The first answer lives in a company database, the second in a contact database,
and only the third is really on the open web. Reaching either of the first two
normally means finding the vendor, signing up, getting a key, learning their
schema, and running it as its own service. Three answers, three integrations.

Exchange puts those providers behind the contract you already use: the same
[`/search`](/api-reference/endpoint/search) call, the same API key. By the end
of this page, triaging that lead is a single request.

<Note>
Searching the catalog adds no credits to a request. Credits are charged when a
capability is executed; a web query alongside it bills as an ordinary search.
</Note>

<Card title="Browse the provider catalogue" icon="table-list" href="https://www.firecrawl.dev/exchange">
  See every supported provider, its capabilities, and what each one costs.
</Card>

## How it works

A provider is a data source: Crunchbase for company and funding records, Apollo
for people, and so on. A capability is one thing that provider can do, such as
searching organizations or finding a person by title. Each capability publishes a
JSON Schema for its options, a schema for its response, and a price in credits,
so an agent can build a valid call and know the cost before it commits. The
[provider catalogue](https://www.firecrawl.dev/exchange) lists everything
currently available.

<CardGroup cols={3}>
  <Card title="Find" icon="compass">
    Set `sources` to `exchange` to search the catalog. Adds no credits.
  </Card>
  <Card title="Call" icon="bolt">
    Add an `exchange` array to run a capability and get a structured result.
  </Card>
  <Card title="Compose" icon="layer-group">
    Return provider results and web results together in one request.
  </Card>
</CardGroup>

## Building the brief

<Steps>
  <Step title="Ask who can answer it">
    Your agent does not need to know Crunchbase or Apollo exist. Name the data it
    needs and set `sources` to `exchange`: matching capabilities come back
    alongside your web results, shaped like any other result. There is no separate
    catalog endpoint, and searching it adds no credits.

    <CodeGroup>
    ```js Node
    const result = await firecrawl.search({
      query: "company funding and people by title",
      sources: ["exchange"],
    });
    ```

    ```python Python
    result = firecrawl.search({
        "query": "company funding and people by title",
        "sources": ["exchange"],
    })
    ```

    ```bash cURL
    curl -X POST "https://api.firecrawl.dev/v2/search" \
      -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "query": "company funding and people by title",
        "sources": ["exchange"]
      }'
    ```
    </CodeGroup>

    Matches arrive under `data.providers`, ranked. The `description` tells you what
    the capability does and what it costs, `provider` and `capability` are the keys
    you call it with, and the `url` resolves to its full request and response
    contract as Markdown. A response looks like this, trimmed to the two
    capabilities this page goes on to use:

    ```json wrap
    {
      "providers": [
        {
          "url": "https://www.firecrawl.dev/exchange/providers/crunchbase/company-search",
          "title": "Crunchbase — Search organizations",
          "description": "Find companies with structured firmographic and funding filters. 5 credits per call. Fetch the url for the full request and response contract.",
          "provider": "crunchbase",
          "capability": "company-search",
          "creditsPerCall": 5
        },
        {
          "url": "https://www.firecrawl.dev/exchange/providers/apollo/people-search",
          "title": "Apollo — Search people",
          "description": "Find net-new prospects using title, seniority, location, and organization filters. 5 credits per call. Fetch the url for the full request and response contract.",
          "provider": "apollo",
          "capability": "people-search",
          "creditsPerCall": 5
        }
      ]
    }
    ```

    Ranking weights a capability name above a provider blurb that merely mentions
    the words. Matching is lexical, so name the data you want rather than the
    situation you are in: "company funding" and "people by title" rank well,
    "research this lead for me" does not.

    To see everything available rather than search it, browse the
    [provider catalogue](https://www.firecrawl.dev/exchange).
  </Step>

  <Step title="Narrow it down (optional)">
    When you already know the shape of the answer, say so. Use the object form of
    the source to scope discovery: `categories` limits it to a cohort, `providers`
    to named providers, and `capabilities` to specific `provider/capability`
    pairs. They compose, so a cohort plus a provider narrows further.

    Filters work with or without a query. Drop the query to browse everything in a
    cohort; add one to rank within it.

    <CodeGroup>
    ```js Node
    const result = await firecrawl.search({
      sources: [
        {
          type: "exchange",
          categories: ["people", "finance"],
        },
      ],
    });
    ```

    ```python Python
    result = firecrawl.search({
        "sources": [
            {
                "type": "exchange",
                "categories": ["people", "finance"],
            }
        ],
    })
    ```

    ```bash cURL
    curl -X POST "https://api.firecrawl.dev/v2/search" \
      -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "query": "enrich a contact",
        "sources": [
          { "type": "exchange", "categories": ["people", "finance"] }
        ]
      }'
    ```
    </CodeGroup>
  </Step>

  <Step title="Get the answer">
    Now fetch the company itself. Providers are called through
    [`/search`](/api-reference/endpoint/search): add an `exchange` array with the
    `provider`, the `capability`, and the `options` its input schema requires.

    `query` is optional. Omit it to call providers only, or include it to get web
    results and provider results back together in one request.

    <CodeGroup>
    ```js Node
    const result = await firecrawl.search({
      exchange: [
        {
          provider: "crunchbase",
          capability: "company-search",
          options: { query: "Firecrawl", limit: 1 },
        },
      ],
    });
    ```

    ```python Python
    result = firecrawl.search({
        "exchange": [
            {
                "provider": "crunchbase",
                "capability": "company-search",
                "options": {"query": "Firecrawl", "limit": 1},
            }
        ]
    })
    ```

    ```bash cURL
    curl -X POST "https://api.firecrawl.dev/v2/search" \
      -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "exchange": [
          {
            "provider": "crunchbase",
            "capability": "company-search",
            "options": { "query": "Firecrawl", "limit": 1 }
          }
        ]
      }'
    ```
    </CodeGroup>

    Provider results arrive under `data.exchange`, and each call is reported
    separately so one failure does not discard the rest:

    ```json wrap
    {
      "success": true,
      "data": {
        "exchange": [
          {
            "provider": "crunchbase",
            "capability": "company-search",
            "delivery": "direct",
            "creditsCost": 5,
            "data": {
              "organizations": [
                { "name": "Firecrawl", "permalink": "firecrawl", "founded_on": "2024-04-01" }
              ]
            }
          }
        ]
      },
      "creditsUsed": 5,
      "id": "0198f3c2-..."
    }
    ```

    A call that fails carries an `error` object with a `code`, a `message`, and
    `retryable` instead of `data`.
  </Step>
</Steps>

## The whole brief, one request

Each step so far answered one part. They compose: the `exchange` array takes up
to ten calls, and a `query` runs a web search alongside them.

So the brief we started with — what Firecrawl is, who works there, and what is
being written about it — is one request across three sources. The company from
Crunchbase, the people from Apollo, the coverage from the web. No extra
accounts, no second endpoint, nothing to reconcile afterwards.

<CodeGroup>
```js Node
const result = await firecrawl.search({
  query: "firecrawl",
  sources: ["news"],
  exchange: [
    {
      provider: "crunchbase",
      capability: "company-search",
      options: { query: "Firecrawl", limit: 1 },
    },
    {
      provider: "apollo",
      capability: "people-search",
      options: {
        query: "engineering leadership at Firecrawl",
        filters: { titles: ["Head of AI", "VP Artificial Intelligence"] },
        limit: 5,
      },
    },
  ],
});

result.news; // coverage from the web
result.exchange; // the company and the people, in call order
```

```python Python
result = firecrawl.search({
    "query": "firecrawl",
    "sources": ["news"],
    "exchange": [
        {
            "provider": "crunchbase",
            "capability": "company-search",
            "options": {"query": "Firecrawl", "limit": 1},
        },
        {
            "provider": "apollo",
            "capability": "people-search",
            "options": {
                "query": "engineering leadership at Firecrawl",
                "filters": {"titles": ["Head of AI", "VP Artificial Intelligence"]},
                "limit": 5,
            },
        },
    ],
})
```

```bash cURL
curl -X POST "https://api.firecrawl.dev/v2/search" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "firecrawl",
    "sources": ["news"],
    "exchange": [
      {
        "provider": "crunchbase",
        "capability": "company-search",
        "options": { "query": "Firecrawl", "limit": 1 }
      },
      {
        "provider": "apollo",
        "capability": "people-search",
        "options": {
          "query": "engineering leadership at Firecrawl",
          "filters": { "titles": ["Head of AI", "VP Artificial Intelligence"] },
          "limit": 5
        }
      }
    ]
  }'
```
</CodeGroup>

Provider results arrive under `data.exchange` in the order they were sent, next
to `data.news` and the other web sources. Each call is reported separately, so
one provider failing does not discard the rest:

```json wrap
{
  "success": true,
  "data": {
    "news": [
      {
        "url": "https://www.example.com/firecrawl-exchange",
        "title": "Firecrawl ships Exchange for agents",
        "date": "2026-07-29"
      }
    ],
    "exchange": [
      {
        "provider": "crunchbase",
        "capability": "company-search",
        "delivery": "direct",
        "creditsCost": 5,
        "data": {
          "organizations": [
            { "name": "Firecrawl", "permalink": "firecrawl", "founded_on": "2024-04-01" }
          ]
        }
      },
      {
        "provider": "apollo",
        "capability": "people-search",
        "delivery": "direct",
        "creditsCost": 5,
        "data": {
          "people": [
            { "name": "Jane Doe", "title": "Head of Engineering", "organization": "Firecrawl" }
          ]
        }
      }
    ]
  },
  "creditsUsed": 11,
  "id": "0198f3c2-..."
}
```

The web search bills as an ordinary search and each capability bills its own
`creditsPerCall`, so the cost of the whole request is the sum of its parts.

## Pricing

Searching the catalog adds no credits. Running a capability costs the
`creditsPerCall` published in its contract, and you are charged only for calls
that succeed. Discovery results carry the price, so you can check before
calling.

## List your data on Exchange

If you have an API you want agents to reach, you can publish it as a provider.
Upload an OpenAPI specification and Firecrawl derives your capabilities from it,
then walks you through pricing, categorisation, and ownership verification.
Every listing is reviewed before it becomes public.

<Card title="List your data" icon="upload" href="https://www.firecrawl.dev/exchange">
  Start from the Exchange directory in your dashboard.
</Card>
