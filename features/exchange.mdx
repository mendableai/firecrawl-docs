---
title: "Exchange"
description: "Call approved data providers through one Firecrawl contract"
og:title: "Exchange | Firecrawl"
og:description: "Call approved data providers through one Firecrawl contract"
icon: "arrow-right-arrow-left"
---

<iframe
  title="An agent answering a question with one Exchange request"
  sandbox="allow-scripts"
  style={{ width: "100%", height: "580px", border: "0", colorScheme: "dark" }}
  srcDoc={`<!doctype html><html><head><meta charset="utf-8"><style>
*{box-sizing:border-box}
html,body{height:100%}
body{margin:0;padding:12px 0 10px;display:flex;flex-direction:column;
  font:13px/1.85 ui-monospace,SFMono-Regular,Menlo,Consolas,"Liberation Mono",monospace;
  background:#26313B;color:#E4E9ED;border-radius:10px;overflow:hidden}
#tabs{display:flex;gap:12px;justify-content:center;margin:0 14px 14px;flex:0 0 auto}
button{position:relative;font:inherit;font-size:11px;letter-spacing:.03em;cursor:pointer;
  padding:0 0 7px;border:0;background:transparent;color:#7C8794;transition:color .2s;
  min-width:78px}
button:hover{color:#C3CCD4}
button.on{color:#E4E9ED}
button .track{position:absolute;left:0;right:0;bottom:0;height:2px;background:#3C4854;
  border-radius:2px;overflow:hidden}
button .seg{display:block;height:100%;width:0;background:#FA5D19;border-radius:2px}
button.on .seg{transition:width .6s cubic-bezier(.4,0,.2,1)}
#b{flex:1 1 auto;min-height:0;overflow:hidden;display:flex;flex-direction:column;
  justify-content:flex-end;padding:0 14px;mask-image:linear-gradient(transparent,#000 8%);
  transition:opacity .55s ease}
#b.fade{opacity:0}
.row,.sp,.user{flex:0 0 auto}
.row{white-space:pre-wrap}
.user{background:#323E49;margin:0 -14px;padding:1px 14px;white-space:pre-wrap;color:#E4E9ED}
.chev{color:#7C8794}
.dot{color:#E4E9ED}
.dotg{color:#7C8794}
.g{color:#94A1AD}
.d{color:#6E7A86}
.ok{color:#63C89B}
.hd{color:#E4E9ED;font-weight:600}
.mk{display:inline-block;width:3ch}
.acc{color:#FA5D19}
.ind{padding-left:20px}
.sp{height:10px}
.live{color:#FA5D19;animation:pulse 1.15s ease-in-out infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.25}}
.cur{display:inline-block;width:7px;height:14px;background:#FA5D19;vertical-align:-2px;
  animation:blink 1.1s steps(2,start) infinite}
@keyframes blink{50%{opacity:0}}
#status{flex:0 0 auto;padding:6px 14px 0;color:#FA5D19;min-height:22px}
</style></head><body>
<div id="tabs"></div>
<div id="b"></div>
<div id="status"></div>
<script>
var SCENES = [
  { id:'Firmographic',
    prompt:'I am meeting someone from Firecrawl this afternoon, jane@firecrawl.dev. Brief me before I go.',
    think:'I have one email address. I need the person behind it, the company, and what has changed recently.',
    findQ:'person by email, company funding, and recent coverage',
    found:[['apollo/people-enrichment','5 credits per call'],
           ['crunchbase/company-search','5 credits per call'],
           ['benzinga/news-search','5 credits per call']],
    callQ:'jane@firecrawl.dev and the company behind it',
    calls:3,
    rows:[['apollo/people-enrichment','Jane Doe · Head of Engineering'],
          ['crunchbase/company-search','Firecrawl · Series A · 2024'],
          ['benzinga/news-search','3 stories · Exchange launch']],
    credits:'15 credits · 5 × 3 calls',
    table:[['','executive brief'],
           ['who','Jane Doe · Head of Engineering'],
           ['tenure','2 years · previously infrastructure'],
           ['company','Firecrawl · Series A · founded 2024'],
           ['team','~15 people · 4 in engineering'],
           ['product','web data infrastructure for agents'],
           ['shipped','Exchange · this month'],
           ['talk track','provider onboarding, agent data access']],
    done:'She runs engineering, the company raised a Series A, and Exchange shipped this month. Lead with provider onboarding.',
    after:'Writing brief.md now…' },
  { id:'Finance',
    prompt:'I am meeting our LPs in ten minutes. Build me a brief on the holdings.',
    think:'Let me start from the portfolio file, then pull live data on each name.',
    read:{ file:'holdings.md', result:'4 positions · NVDA, AAPL, MSFT, AMD' },
    findQ:'market data, sec filings, and market news',
    found:[['alpha-vantage/quote','5 credits per call'],
           ['financial-datasets/sec-filings','5 credits per call'],
           ['benzinga/news-search','5 credits per call']],
    callQ:'NVDA, AAPL, MSFT, AMD prices, filings, and coverage',
    calls:3,
    rows:[['alpha-vantage/quote','4 symbols · live'],
          ['financial-datasets/sec-filings','4 filings · all 10-Q'],
          ['benzinga/news-search','21 stories · earnings and guidance']],
    credits:'15 credits · 5 × 3 calls',
    table:[['','NVIDIA','Apple','Microsoft','AMD'],
           ['price','$184.62','$271.84','$512.30','$164.90'],
           ['revenue','$130.5B','$391.0B','$281.7B','$25.8B'],
           ['last 10-Q','24 Jul','31 Jul','29 Jul','5 Aug'],
           ['coverage','8 stories','4 stories','6 stories','3 stories']],
    done:'Apple leads on revenue and NVIDIA on attention, while AMD is a tenth the size of either. All four filed 10-Qs in the last two weeks.',
    after:'Building the LP deck now…' },
  { id:'News',
    prompt:'I need a reading list on agent infrastructure for tomorrow morning.',
    think:'I want reporting with metadata, not a page scrape.',
    findQ:'long-form reporting and market news',
    found:[['the-atlantic/article-search','5 credits per call'],
           ['benzinga/news-search','5 credits per call']],
    callQ:'agent infrastructure coverage',
    calls:2,
    rows:[['the-atlantic/article-search','4 articles · 100-word previews'],
          ['benzinga/news-search','12 stories · launches and funding']],
    credits:'10 credits · 5 × 2 calls',
    done:'The Atlantic has four long-form pieces this month, mostly on reliability and cost. Benzinga adds twelve, mostly launches and funding.',
    after:'Writing reading-list.md now…' },
  { id:'Hiring',
    prompt:'Which of our portfolio companies are actually scaling engineering?',
    think:'Let me see who is advertising roles first, then check that against provider data.',
    pre:[{ tool:'Search', args:'("portfolio companies hiring infrastructure engineers")', result:'12 results · careers pages' },
         { tool:'Scrape', args:'(northwind.com/careers)', result:'9 open roles · 6 in engineering' }],
    findQ:'workforce signals and headcount by company',
    found:[['apollo/organization-search','5 credits per call'],
           ['apollo/people-search','5 credits per call']],
    callQ:'headcount and engineering hires across the portfolio',
    calls:2,
    rows:[['apollo/organization-search','6 companies · headcount bands'],
          ['apollo/people-search','38 engineers hired in 90 days']],
    credits:'10 credits · 5 × 2 calls',
    table:[['','Headcount','Eng hires','Open roles'],
           ['Northwind','142','+18','9'],
           ['Belltower','61','+12','4'],
           ['Cadence','absent','0','0'],
           ['Kestrel','24','+8','6']],
    done:'Northwind and Belltower are scaling fastest, adding thirty engineers between them, and Kestrel is small but growing. Cadence shows no headcount signal and no hires in ninety days, which contradicts the deck. Worth raising before the board reads it.',
    after:'Writing the board update now…' },
  { id:'Marketing',
    prompt:'We launch next week. Build the target audience and enrich the first hundred contacts.',
    think:'I need the audience defined first, then real people to put in it.',
    findQ:'audience building and contact enrichment',
    found:[['wattdata/audience-compose','5 credits per call'],
           ['wattdata/audience-insights','5 credits per call'],
           ['fullenrich/contact-search','5 credits per call']],
    callQ:'infrastructure buyers evaluating agent tooling',
    calls:3,
    rows:[['wattdata/audience-compose','1 audience · 12.4k profiles'],
          ['wattdata/audience-insights','top traits · infra buyers'],
          ['fullenrich/contact-search','100 contacts · verified']],
    credits:'15 credits · 5 × 3 calls',
    done:'The audience is twelve thousand strong, skewed to platform teams, and the first hundred contacts are verified and ready.',
    after:'Writing launch-list.csv now…' },
];

var SPIN = ['✻','✳','✶','✻'];
var WORDS = ['Kindling','Smouldering','Crackling','Stoking','Crawling','Percolating','Simmering'];
var host = document.getElementById('b');
var tabs = document.getElementById('tabs');
var statusEl = document.getElementById('status');
var run = 0, tokens = 0, t0 = 0, statusTimer = null, word = 'Flowing';

function wait(ms){ return new Promise(function(r){ setTimeout(r, ms); }); }

var sceneIndex = 0;
function segs(){ return tabs.querySelectorAll('.seg'); }

// The labels double as the loader: the running one fills, earlier ones stay
// full, later ones sit empty.
function mark(f){
  var all = segs();
  for (var i = 0; i < all.length; i++){
    all[i].style.width = i < sceneIndex ? '100%' : (i === sceneIndex ? (f * 100).toFixed(1) + '%' : '0%');
  }
}
function el(cls, mine){
  var d = document.createElement('div');
  d.className = (cls || 'row');
  // A superseded run keeps a detached node so it can finish writing harmlessly.
  if (mine !== undefined && run !== mine) return d;
  host.appendChild(d); trim(); return d;
}
function gap(mine){ if (mine !== undefined && run !== mine) return; var d = document.createElement('div'); d.className = 'sp'; host.appendChild(d); }
function pad(s, n){ while (s.length < n) s += ' '; return s; }
function fmt(n){ return n >= 1000 ? (n / 1000).toFixed(1) + 'k' : String(n); }

// The persistent status line, the way the CLI keeps time and tokens on screen.
function startStatus(){
  t0 = Date.now(); tokens = 0; var f = 0;
  clearInterval(statusTimer);
  statusTimer = setInterval(function(){
    var e = Math.round((Date.now() - t0) / 1000);
    var clock = e < 60 ? e + 's' : Math.floor(e / 60) + 'm ' + (e % 60) + 's';
    tokens += Math.round(Math.random() * 45);
    statusEl.innerHTML = '<span>' + SPIN[f++ % 4] + ' ' + word + '… </span><span class="d">(' +
      clock + ' · ↓ ' + fmt(tokens) + ' tokens · esc to interrupt)</span>';
  }, 120);
}
function stopStatus(){ clearInterval(statusTimer); statusEl.innerHTML = ''; }

async function stream(target, segs, cps, mine){
  for (var i = 0; i < segs.length; i++){
    var span = document.createElement('span');
    if (segs[i][1]) span.className = segs[i][1];
    target.appendChild(span);
    var txt = segs[i][0];
    for (var j = 0; j < txt.length; j++){
      if (run !== mine) return;
      span.textContent += txt[j];
      if (j % 2 === 0) await wait(cps);
    }
  }
}

// A tool call renders as "Calling…" and is rewritten in place to "Called".
function toolLine(name, times, mine){
  var d = el('row', mine);
  var suffix = times > 1 ? ' ' + times + ' times' : '';
  d.innerHTML = '<span class="live">●</span> <span class="g">Calling ' + name + suffix +
    '… </span><span class="d">(ctrl+o to expand)</span>';
  return { node: d, name: name, suffix: suffix };
}
function toolDone(t){
  t.node.innerHTML = '<span class="dot">●</span> <span>Called ' + t.name + t.suffix +
    '</span> <span class="d">(ctrl+o to expand)</span>';
}

// Old turns are kept so the panel reads as one continuing session rather than
// resetting to an empty box, but the transcript is trimmed so it cannot grow
// without bound.
function trim(){
  while (host.children.length > 42) host.removeChild(host.firstChild);
}

// Draws a bordered table, the way a CLI renders one.
function repeat(ch, n){ var out = ''; for (var i = 0; i < n; i++) out += ch; return out; }

async function renderTable(rows, mine){
  var w = [];
  rows.forEach(function(r){
    r.forEach(function(cell, i){ if (!w[i] || cell.length > w[i]) w[i] = cell.length; });
  });

  function rule(l, m, r){
    var out = l;
    for (var i = 0; i < w.length; i++){
      out += repeat('─', w[i] + 2) + (i === w.length - 1 ? r : m);
    }
    return out;
  }
  // Rows are built as nodes rather than by rewriting markup, so a cell can
  // never be mistaken for the border spans around it.
  function paint(target, row, cls){
    function bar(){
      var b = document.createElement('span');
      b.className = 'd';
      b.textContent = '│';
      target.appendChild(b);
    }
    bar();
    for (var i = 0; i < w.length; i++){
      var c = document.createElement('span');
      if (cls) c.className = cls;
      c.textContent = ' ' + pad(row[i], w[i]) + ' ';
      target.appendChild(c);
      bar();
    }
  }

  var top = el('row ind d', mine); top.textContent = rule('┌', '┬', '┐');
  await wait(90); if (run !== mine) return;

  var head = el('row ind', mine);
  paint(head, rows[0], 'hd');
  await wait(130); if (run !== mine) return;

  var mid = el('row ind d', mine); mid.textContent = rule('├', '┼', '┤');
  await wait(110); if (run !== mine) return;

  for (var i = 1; i < rows.length; i++){
    var line = el('row ind', mine);
    paint(line, rows[i], '');
    await wait(130); if (run !== mine) return;
  }

  var bot = el('row ind d', mine); bot.textContent = rule('└', '┴', '┘');
  await wait(200);
}

async function play(scene, mine){
  word = WORDS[Math.floor(Math.random() * WORDS.length)];

  var u = el('user', mine);
  u.innerHTML = '<span class="chev">〉</span> ';
  await stream(u, [[scene.prompt, '']], 20, mine);
  await wait(950); if (run !== mine) return;
  mark(0.08);
  startStatus();

  gap(mine);
  var a = el('row', mine); a.innerHTML = '<span class="live">●</span> ';
  await stream(a, [[scene.think, '']], 14, mine);
  a.firstChild.className = 'dot';
  await wait(900); if (run !== mine) return;

  // A local file read first, so Exchange is shown filling gaps in data the
  // agent already has rather than starting from nothing.
  var pre = scene.pre || (scene.read ? [{ tool:'Read', args:'(' + scene.read.file + ')', result: scene.read.result }] : []);
  for (var pi = 0; pi < pre.length; pi++){
    gap(mine);
    var pd = el('row', mine); pd.innerHTML = '<span class="live">●</span> ';
    await stream(pd, [[pre[pi].tool, 't'], [pre[pi].args, 'g']], 9, mine);
    await wait(600); if (run !== mine) return;
    pd.firstChild.className = 'dot';
    var pr2 = el('row ind g', mine);
    pr2.innerHTML = '<span class="d mk">⎿</span>';
    await stream(pr2, [[pre[pi].result, 'g']], 8, mine);
    await wait(700); if (run !== mine) return;
  }

  gap(mine);
  var t1 = toolLine('firecrawl_exchange', 1, mine);
  var q1 = el('row ind g', mine);
  q1.innerHTML = '<span class="d mk">⎿</span>';
  await stream(q1, [['"' + scene.findQ + '"', 'd']], 11, mine);
  await wait(2300); if (run !== mine) return;
  toolDone(t1);
  q1.innerHTML = '<span class="d mk">⎿</span><span class="g">found ' + scene.found.length + ' ' +
    (scene.found.length === 1 ? 'capability' : 'capabilities') + ' · 0 credits</span>';
  var fw = 0;
  scene.found.forEach(function(r){ if (r[0].length > fw) fw = r[0].length; });
  fw += 2;
  for (var k = 0; k < scene.found.length; k++){
    var fr = el('row ind g', mine); fr.innerHTML = '<span class="mk"></span>';
    await stream(fr, [[pad(scene.found[k][0], fw), 'd'], [scene.found[k][1], 'd']], 8, mine);
    await wait(420); if (run !== mine) return;
  }
  await wait(1100); if (run !== mine) return;
  mark(0.42);

  gap(mine);
  var t2 = toolLine('firecrawl_exchange', scene.calls, mine);
  var q2 = el('row ind g', mine);
  q2.innerHTML = '<span class="d mk">⎿</span>';
  await stream(q2, [['"' + scene.callQ + '"', 'd']], 11, mine);
  await wait(3000); if (run !== mine) return;
  toolDone(t2);

  var w = 0;
  scene.rows.forEach(function(r){ if (r[0].length > w) w = r[0].length; });
  w += 2;
  q2.innerHTML = '<span class="d mk">⎿</span>';
  await stream(q2, [[pad(scene.rows[0][0], w), 'g'], [scene.rows[0][1], 'g']], 8, mine);
  await wait(560); if (run !== mine) return;
  for (var i = 1; i < scene.rows.length; i++){
    var r = el('row ind g', mine); r.innerHTML = '<span class="mk"></span>';
    await stream(r, [[pad(scene.rows[i][0], w), 'g'], [scene.rows[i][1], 'g']], 8, mine);
    await wait(560); if (run !== mine) return;
  }
  var cr = el('row ind d', mine); cr.innerHTML = '<span class="mk"></span>';
  await stream(cr, [[scene.credits, 'd']], 8, mine);
  mark(0.74);
  await wait(1200); if (run !== mine) return;

  if (scene.table){
    mark(0.84);
    gap(mine);
    await renderTable(scene.table, mine);
    if (run !== mine) return;
    await wait(900);
  }

  stopStatus();
  gap(mine);
  var d = el('row', mine); d.innerHTML = '<span class="live">●</span> ';
  await stream(d, [[scene.done, '']], 5, mine);
  d.firstChild.className = 'dot';
  await wait(700); if (run !== mine) return;

  mark(1);
  var n = el('row', mine); n.innerHTML = '<span class="live">●</span> ';
  await stream(n, [[scene.after, 'g']], 14, mine);
  var cur = document.createElement('span'); cur.className = 'cur'; n.appendChild(cur);
  await wait(1800); if (run !== mine) return;

  // Fade the whole turn out before the next scenario takes over.
  host.className = 'fade';
  await wait(650); if (run !== mine) return;
  host.innerHTML = '';
  host.className = '';
  await wait(350);
}

function highlight(idx){
  [].forEach.call(tabs.children, function(btn, i){ btn.className = i === idx ? 'on' : ''; });
}

// Each scene plays once, then hands over to the next, so the panel walks the
// whole catalog on its own.
function select(idx){
  run++; stopStatus(); host.innerHTML = ''; host.className = '';
  sceneIndex = idx; mark(0);
  var mine = run, current = idx;
  (async function(){
    while (run === mine){
      highlight(current);
      sceneIndex = current;
      if (current === 0) mark(0);
      await play(SCENES[current], mine);
      if (run !== mine) return;
      current = (current + 1) % SCENES.length;
    }
  })();
}

SCENES.forEach(function(sc, i){
  var btn = document.createElement('button');
  btn.appendChild(document.createTextNode(sc.id));
  var track = document.createElement('span');
  track.className = 'track';
  var seg = document.createElement('i');
  seg.className = 'seg';
  track.appendChild(seg);
  btn.appendChild(track);
  btn.onclick = function(){ select(i); };
  tabs.appendChild(btn);
});
select(1);
</script></body></html>`}
/>

A lead lands from someone at Firecrawl. Before anyone replies, your agent needs
three things the web will not give you cleanly:

1. Is the company real, funded, and how big?
2. Who works there and at what level?
3. What have they shipped in the last month?

The first answer lives in a company database, the second in a contact database,
and only the third is really on the open web. Reaching either of the first two
normally means finding the vendor, signing up, getting a key, learning their
schema, and running it as its own service. Three answers, three integrations.

Exchange puts those providers behind the contract you already use: the same
[`/search`](/api-reference/endpoint/search) call, the same API key. By the end
of this page, triaging that lead is a single request.

<Note>
Searching the catalog adds no credits to a request. Credits are charged when a
capability is executed; a web query alongside it bills as an ordinary search.
</Note>

<Card title="Browse the provider catalogue" icon="table-list" href="https://www.firecrawl.dev/exchange">
  See every supported provider, its capabilities, and what each one costs.
</Card>

## How it works

A provider is a data source: Crunchbase for company and funding records, Apollo
for people, and so on. A capability is one thing that provider can do, such as
searching organizations or finding a person by title. Each capability publishes a
JSON Schema for its options, a schema for its response, and a price in credits,
so an agent can build a valid call and know the cost before it commits. The
[provider catalogue](https://www.firecrawl.dev/exchange) lists everything
currently available.

<CardGroup cols={3}>
  <Card title="Find" icon="compass">
    Set `sources` to `exchange` to search the catalog. Adds no credits.
  </Card>
  <Card title="Call" icon="bolt">
    Add an `exchange` array to run a capability and get a structured result.
  </Card>
  <Card title="Compose" icon="layer-group">
    Return provider results and web results together in one request.
  </Card>
</CardGroup>

## Finding and calling capabilities

<Steps>
  <Step title="Ask who can answer it">
    Your agent does not need to know Crunchbase or Apollo exist. Name the data it
    needs and set `sources` to `exchange`: matching capabilities come back
    alongside your web results, shaped like any other result. There is no separate
    catalog endpoint, and searching it adds no credits.

    <CodeGroup>
    ```js Node
    const result = await firecrawl.search({
      query: "company funding and people by title",
      sources: ["exchange"],
    });
    ```

    ```python Python
    result = firecrawl.search({
        "query": "company funding and people by title",
        "sources": ["exchange"],
    })
    ```

    ```bash cURL
    curl -X POST "https://api.firecrawl.dev/v2/search" \
      -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "query": "company funding and people by title",
        "sources": ["exchange"]
      }'
    ```
    </CodeGroup>

    Matches arrive under `data.providers`, ranked. The `description` tells you what
    the capability does and what it costs, `provider` and `capability` are the keys
    you call it with, and the `url` resolves to its full request and response
    contract as Markdown. A response looks like this, trimmed to the two
    capabilities this page goes on to use:

    ```json wrap
    {
      "providers": [
        {
          "url": "https://www.firecrawl.dev/exchange/providers/crunchbase/company-search",
          "title": "Crunchbase — Search organizations",
          "description": "Find companies with structured firmographic and funding filters. 5 credits per call. Fetch the url for the full request and response contract.",
          "provider": "crunchbase",
          "capability": "company-search",
          "creditsPerCall": 5
        },
        {
          "url": "https://www.firecrawl.dev/exchange/providers/apollo/people-search",
          "title": "Apollo — Search people",
          "description": "Find net-new prospects using title, seniority, location, and organization filters. 5 credits per call. Fetch the url for the full request and response contract.",
          "provider": "apollo",
          "capability": "people-search",
          "creditsPerCall": 5
        }
      ]
    }
    ```

    Ranking weights a capability name above a provider blurb that merely mentions
    the words. Matching is lexical, so name the data you want rather than the
    situation you are in: "company funding" and "people by title" rank well,
    "research this lead for me" does not.

    To see everything available rather than search it, browse the
    [provider catalogue](https://www.firecrawl.dev/exchange).
  </Step>

  <Step title="Narrow it down (optional)">
    When you already know the shape of the answer, say so. Use the object form of
    the source to scope discovery: `categories` limits it to a cohort, `providers`
    to named providers, and `capabilities` to specific `provider/capability`
    pairs. They compose, so a cohort plus a provider narrows further.

    Filters work with or without a query. Drop the query to browse everything in a
    cohort; add one to rank within it.

    <CodeGroup>
    ```js Node
    const result = await firecrawl.search({
      sources: [
        {
          type: "exchange",
          categories: ["people", "finance"],
        },
      ],
    });
    ```

    ```python Python
    result = firecrawl.search({
        "sources": [
            {
                "type": "exchange",
                "categories": ["people", "finance"],
            }
        ],
    })
    ```

    ```bash cURL
    curl -X POST "https://api.firecrawl.dev/v2/search" \
      -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "query": "enrich a contact",
        "sources": [
          { "type": "exchange", "categories": ["people", "finance"] }
        ]
      }'
    ```
    </CodeGroup>
  </Step>

  <Step title="Get the answer">
    Now fetch the company itself. Providers are called through
    [`/search`](/api-reference/endpoint/search): add an `exchange` array with the
    `provider`, the `capability`, and the `options` its input schema requires.

    `query` is optional. Omit it to call providers only, or include it to get web
    results and provider results back together in one request.

    <CodeGroup>
    ```js Node
    const result = await firecrawl.search({
      exchange: [
        {
          provider: "crunchbase",
          capability: "company-search",
          options: { query: "Firecrawl", limit: 1 },
        },
      ],
    });
    ```

    ```python Python
    result = firecrawl.search({
        "exchange": [
            {
                "provider": "crunchbase",
                "capability": "company-search",
                "options": {"query": "Firecrawl", "limit": 1},
            }
        ]
    })
    ```

    ```bash cURL
    curl -X POST "https://api.firecrawl.dev/v2/search" \
      -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "exchange": [
          {
            "provider": "crunchbase",
            "capability": "company-search",
            "options": { "query": "Firecrawl", "limit": 1 }
          }
        ]
      }'
    ```
    </CodeGroup>

    Provider results arrive under `data.exchange`, and each call is reported
    separately so one failure does not discard the rest:

    ```json wrap
    {
      "success": true,
      "data": {
        "exchange": [
          {
            "provider": "crunchbase",
            "capability": "company-search",
            "delivery": "direct",
            "creditsCost": 5,
            "data": {
              "organizations": [
                { "name": "Firecrawl", "permalink": "firecrawl", "founded_on": "2024-04-01" }
              ]
            }
          }
        ]
      },
      "creditsUsed": 5,
      "id": "0198f3c2-..."
    }
    ```

    A call that fails carries an `error` object with a `code`, a `message`, and
    `retryable` instead of `data`.
  </Step>
</Steps>

## Multiple providers in one request

Each step so far answered one part. They compose: the `exchange` array takes up
to ten calls, and a `query` runs a web search alongside them.

So the brief we started with — what Firecrawl is, who works there, and what is
being written about it — is one request across three sources. The company from
Crunchbase, the people from Apollo, the coverage from the web. No extra
accounts, no second endpoint, nothing to reconcile afterwards.

<CodeGroup>
```js Node
const result = await firecrawl.search({
  query: "firecrawl",
  sources: ["news"],
  exchange: [
    {
      provider: "crunchbase",
      capability: "company-search",
      options: { query: "Firecrawl", limit: 1 },
    },
    {
      provider: "apollo",
      capability: "people-search",
      options: {
        query: "engineering leadership at Firecrawl",
        filters: { titles: ["Head of AI", "VP Artificial Intelligence"] },
        limit: 5,
      },
    },
  ],
});

result.news; // coverage from the web
result.exchange; // the company and the people, in call order
```

```python Python
result = firecrawl.search({
    "query": "firecrawl",
    "sources": ["news"],
    "exchange": [
        {
            "provider": "crunchbase",
            "capability": "company-search",
            "options": {"query": "Firecrawl", "limit": 1},
        },
        {
            "provider": "apollo",
            "capability": "people-search",
            "options": {
                "query": "engineering leadership at Firecrawl",
                "filters": {"titles": ["Head of AI", "VP Artificial Intelligence"]},
                "limit": 5,
            },
        },
    ],
})
```

```bash cURL
curl -X POST "https://api.firecrawl.dev/v2/search" \
  -H "Authorization: Bearer $FIRECRAWL_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "firecrawl",
    "sources": ["news"],
    "exchange": [
      {
        "provider": "crunchbase",
        "capability": "company-search",
        "options": { "query": "Firecrawl", "limit": 1 }
      },
      {
        "provider": "apollo",
        "capability": "people-search",
        "options": {
          "query": "engineering leadership at Firecrawl",
          "filters": { "titles": ["Head of AI", "VP Artificial Intelligence"] },
          "limit": 5
        }
      }
    ]
  }'
```
</CodeGroup>

Provider results arrive under `data.exchange` in the order they were sent, next
to `data.news` and the other web sources. Each call is reported separately, so
one provider failing does not discard the rest:

```json wrap
{
  "success": true,
  "data": {
    "news": [
      {
        "url": "https://www.example.com/firecrawl-exchange",
        "title": "Firecrawl ships Exchange for agents",
        "date": "2026-07-29"
      }
    ],
    "exchange": [
      {
        "provider": "crunchbase",
        "capability": "company-search",
        "delivery": "direct",
        "creditsCost": 5,
        "data": {
          "organizations": [
            { "name": "Firecrawl", "permalink": "firecrawl", "founded_on": "2024-04-01" }
          ]
        }
      },
      {
        "provider": "apollo",
        "capability": "people-search",
        "delivery": "direct",
        "creditsCost": 5,
        "data": {
          "people": [
            { "name": "Jane Doe", "title": "Head of Engineering", "organization": "Firecrawl" }
          ]
        }
      }
    ]
  },
  "creditsUsed": 11,
  "id": "0198f3c2-..."
}
```

The web search bills as an ordinary search and each capability bills its own
`creditsPerCall`, so the cost of the whole request is the sum of its parts.

## Pricing

Searching the catalog adds no credits. Running a capability costs the
`creditsPerCall` published in its contract, and you are charged only for calls
that succeed. Discovery results carry the price, so you can check before
calling.

## Listing your data

If you have an API you want agents to reach, you can publish it as a provider.
Upload an OpenAPI specification and Firecrawl derives your capabilities from it,
then walks you through pricing, categorisation, and ownership verification.
Every listing is reviewed before it becomes public.

<Card title="List your data" icon="upload" href="https://www.firecrawl.dev/exchange">
  Start from the Exchange directory in your dashboard.
</Card>
