---
title: 'JSON 模式 - LLM 抽取'
description: '通过 LLM 从页面提取结构化数据'
og:title: "JSON 模式 | Firecrawl"
og:description: "通过 LLM 从页面提取结构化数据"
sidebarTitle: "JSON 模式"
---

import ExtractCURL from "/snippets/zh/v1/llm-extract/base/curl.mdx";
import ExtractPython from "/snippets/zh/v1/llm-extract/base/python.mdx";
import ExtractNode from "/snippets/zh/v1/llm-extract/base/js.mdx";
import ExtractOutput from "/snippets/zh/v1/llm-extract/base/output.mdx";
import ExtractNoSchemaCURL from "/snippets/zh/v1/llm-extract/no-schema/curl.mdx";
import ExtractNoSchemaOutput from "/snippets/zh/v1/llm-extract/no-schema/output.mdx";

<div id="scrape-and-extract-structured-data-with-firecrawl">
  ## 使用 Firecrawl 抓取并提取结构化数据
</div>

{/* <Warning>Scrape LLM Extract 将在未来版本中弃用。请使用新的 [Extract](/features/extract) 端点。</Warning> */}

Firecrawl 使用 AI 通过 3 个步骤从网页获取结构化数据：

1. **设置 Schema：**
   通过定义 JSON schema (采用 OpenAI 的格式) 并提供网页 URL，告知我们你需要的数据。

2. **发起请求：**
   将你的 URL 和 schema 发送到我们的 /scrape 端点。查看方法：
   [Scrape Endpoint Documentation](https://docs.firecrawl.dev/api-reference/endpoint/scrape)

3. **获取数据：**
   返回与 schema 匹配的干净、结构化数据，可立即使用。

这让你能够快速、轻松地以所需的格式获取网页数据。

<div id="extract-structured-data">
  ## 提取结构化数据
</div>

<div id="scrape-with-json-endpoint">
  ### /scrape (使用 json) 端点
</div>

用于从抓取的页面中提取结构化数据。

<CodeGroup>
  <ExtractPython />

  <ExtractNode />

  <ExtractCURL />
</CodeGroup>

输出：

<ExtractOutput />

<div id="extracting-without-schema-new">
  ### 无需 schema 的提取 (新) 
</div>

你现在可以只向端点传入一个 `prompt`，在不提供 schema 的情况下完成提取。LLM 会自行决定数据结构。

<CodeGroup>
  <ExtractNoSchemaCURL />
</CodeGroup>

输出：

<ExtractNoSchemaOutput />

<div id="json-options-object">
  ### JSON 选项对象
</div>

`jsonOptions` 对象可接受以下参数：

* `schema`：用于提取的 schema。
* `systemPrompt`：用于提取的系统 prompt。
* `prompt`：在未提供 schema 时用于提取的 prompt。
* `checkPromptInjection`：可选布尔值 (默认值为 `false`) 。启用后，会在运行提取前扫描抓取的页面内容，以检测 prompt 注入尝试。如果检测到注入，请求将失败，并返回 403 状态码和错误代码 `SCRAPE_PROMPT_INJECTION_DETECTED`。运行检查时会额外消耗 4 个额度。请参见[prompt 注入检测](/zh/features/llm-extract#prompt-injection-detection)。