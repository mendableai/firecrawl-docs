---
title: "Depura Firecrawl con Ask"
description: "Depura un trabajo fallido o cualquier problema de integración de Firecrawl con una API de soporte con agentes"
og:title: "Ask | Firecrawl"
og:description: "Depura trabajos fallidos y problemas de scraping, y obtén correcciones verificadas, todo mediante una sola llamada a la API."
sidebarTitle: "Depura con Ask"
icon: "messages-question"
---

Firecrawl `/support/ask` es un agente de soporte con IA disponible como API. Describe tu problema y recibe un diagnóstico verificado con parámetros de corrección prácticos, normalmente en 15–30 segundos.

**Piensa en `/support/ask` como un ingeniero sénior de Firecrawl de guardia para tu agente.**

<Info>
  La API de Ask está diseñada principalmente para **agentes de IA que la consumen**. Si estás creando agentes que usan Firecrawl para scraping, rastreo o extracción de datos, integra `/support/ask` en tu flujo de manejo de errores para resolver problemas de forma autónoma.
</Info>

<div id="two-endpoints">
  ## Dos endpoints
</div>

| Endpoint                    | Autenticación                | Para quién es              | Qué hace                                                    |
| --------------------------- | ---------------------------- | -------------------------- | ----------------------------------------------------------- |
| `POST /support/ask`         | Tu clave de API de Firecrawl | Tus agentes y aplicaciones | Ciclo completo de diagnóstico limitado a tu equipo          |
| `POST /support/docs-search` | Tu clave de API de Firecrawl | Tus agentes y aplicaciones | Respuestas basadas en la documentación pública de Firecrawl |

<div id="quick-start">
  ## Inicio rápido
</div>

<div id="debug-a-failing-crawl">
  ### Depurar un rastreo que falla
</div>

```bash
curl -X POST https://api.firecrawl.dev/v2/support/ask \
  -H "Authorization: Bearer fc-YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "my crawl returned 3 pages but I expected 50"
  }'
```

<div id="search-the-docs">
  ### Buscar en la documentación
</div>

```bash
curl -X POST https://api.firecrawl.dev/v2/support/docs-search \
  -H "Authorization: Bearer fc-YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "how do I set up webhook signature verification?"
  }'
```

<div id="debug-a-failed-job">
  ## Depura un trabajo fallido
</div>

Todos los trabajos de Firecrawl —scraping, rastreo, extracción por lotes, search, mapeo o extracción— se pueden depurar con `/support/ask`. Describe el fallo en lenguaje sencillo e incluye el ID de trabajo si lo tienes; el agente consulta los registros de ese trabajo y el estado de tu cuenta antes de responder.

```bash
curl -X POST https://api.firecrawl.dev/v2/support/ask \
  -H "Authorization: Bearer fc-YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "debug failed job 0f8c9a1b-4e2d-47a1-9c3f-1b2d3e4f5a6b — crawl of https://example.com failed after 12 pages",
    "rationale": "user needs the full docs site indexed before their demo"
  }'
```

Incluye toda la información que tengas; cada dato acota el diagnóstico:

| Detalle                             | Por qué ayuda                                                                                                    |
| ----------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| ID de trabajo                       | Permite al agente consultar directamente los registros, el estado y los resultados por página de ese trabajo     |
| URL de destino                      | Identifica bloqueadores específicos del sitio, como protección contra bots, renderizado de JS o reglas de robots |
| Mensaje de error o código de estado | Distingue los límites de tasa y el agotamiento de créditos de los fallos de scraping                             |
| Lo que esperabas                    | Distingue un fallo crítico de un trabajo que &quot;tuvo éxito&quot; pero sin contenido                           |
| `rationale`                         | Indica al agente qué busca el usuario final para que priorice la evidencia adecuada                              |

<div id="what-ask-checks-for-common-failures">
  ### Qué comprueba Ask ante fallos comunes
</div>

| Síntoma                                            | Qué investiga el agente                                                                                             |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| Estado del trabajo `failed`                        | Registros del trabajo, código de estado HTTP ascendente, historial de proxy y reintentos                            |
| El rastreo devolvió menos páginas de las esperadas | `limit`, `maxDiscoveryDepth`, `includePaths`/`excludePaths`, cobertura del mapa del sitio, reglas de robots         |
| Markdown vacío o truncado                          | Renderizado del lado del cliente, tiempo de espera de `waitFor`, `actions` necesarias, recorte de `onlyMainContent` |
| Respuestas `401` / `402` / `429`                   | Validez y restricciones de la clave de API, créditos restantes, límites de tasa del plan                            |
| Trabajo bloqueado o con tiempo de espera agotado   | Estado de la cola, tiempos de espera por página, concurrencia de trabajos de tu plan                                |
| El Webhook nunca se activó                         | Intentos de entrega, respuestas del endpoint, errores de verificación de firma                                      |

¿No tienes un ID de trabajo? Pasa el cursor sobre la URL de una fila en [Activity Logs](https://www.firecrawl.dev/app/logs) y haz clic en **Copy ID**, o usa el `id` devuelto al iniciar el trabajo.

<div id="debug-from-activity-logs">
  ### Depurar desde los registros de actividad
</div>

Si prefieres no hacer la llamada tú mismo, el dashboard ejecuta el mismo agente por ti. Abre los [registros de actividad](https://www.firecrawl.dev/app/logs) y busca el botón con destellos en la columna **Acciones** de una fila fallida; su texto emergente dice **Depurar incidencia**. Solo aparece en trabajos que fallaron o finalizaron con errores en solicitudes secundarias, por lo que los trabajos correctos o en curso no lo tendrán.

Al hacer clic, el diagnóstico se inicia de inmediato; no hay ningún prompt que escribir. Firecrawl envía la URL, el endpoint, el estado, el mensaje de error y los parámetros de scraping de ese trabajo al mismo agente que está detrás de `/support/ask`, que luego lee los registros del trabajo y el estado de tu cuenta. El contenido de las páginas extraídas nunca se incluye.

El panel que se abre muestra:

| Elemento               | Qué es                                                                                              |
| ---------------------- | --------------------------------------------------------------------------------------------------- |
| Diagnóstico            | La explicación del agente sobre qué salió mal y qué cambiar                                         |
| Indicador de confianza | Alta, media o baja: el nivel de certeza del agente sobre la respuesta                               |
| Indicador **Validado** | Se muestra cuando el agente probó su propia solución sugerida y la prueba se completó correctamente |
| Solución sugerida      | Los parámetros corregidos en formato JSON, con un botón para copiar; pégalos en tu próxima llamada  |
| Fuentes                | Enlaces a las páginas de documentación en las que se basa la respuesta                              |

Si el diagnóstico no lo resuelve, **Abrir ticket de soporte** al final del panel crea un ticket con el análisis del agente ya adjunto, para que no tengas que volver a explicar el fallo.

<Info>
  La depuración desde el dashboard está limitada a 30 ejecuciones por hora por equipo, y tu equipo necesita al menos una clave de API; el agente se ejecuta con tu propia clave, por lo que solo puede ver tus trabajos.
</Info>

Una vez que tengas un diagnóstico, aplica los `fixParameters` devueltos y vuelve a intentarlo; consulta el [patrón de reintento del agente](#agent-retry-pattern) a continuación.

<div id="how-it-works">
  ## Cómo funciona
</div>

Cuando llamas a `/support/ask`, el agente de IA:

1. **Recopila evidencia** — inspecciona en paralelo los registros de tu trabajo, el estado de tu cuenta, el uso de créditos y la documentación relevante
2. **Diagnostica el problema** — analiza toda la evidencia para identificar la causa raíz
3. **Propone una solución** — genera `fixParameters` accionables por máquina que puedes aplicar directamente en tu próxima llamada a la API
4. **Valida la solución** — cuando es posible, prueba la solución con la API de Firecrawl en tiempo real (p. ej., reintentando un scraping con parámetros ajustados) e informa del resultado

<div id="using-ask-in-your-agent">
  ## Usar Ask en tu agente
</div>

Patrón de diseño clave: llama a `/support/ask` cuando tu llamada a la API de Firecrawl falle o devuelva resultados inesperados, y luego usa `fixParameters` para volver a intentarlo.

<div id="python-example">
  ### Ejemplo en Python
</div>

```python
import requests

FIRECRAWL_API_KEY = "fc-YOUR_API_KEY"

def diagnose_firecrawl_issue(question, rationale=None):
    """Call the Firecrawl Ask API to debug an issue."""
    payload = {"question": question}
    if rationale:
        payload["rationale"] = rationale

    response = requests.post(
        "https://api.firecrawl.dev/v2/support/ask",
        headers={
            "Authorization": f"Bearer {FIRECRAWL_API_KEY}",
            "Content-Type": "application/json",
        },
        json=payload,
    )
    return response.json()


# Ejemplo: depurar un scraping que devolvió contenido vacío
result = diagnose_firecrawl_issue(
    question="scrape returned empty markdown for https://example.com",
    rationale="user needs product pricing data for competitive analysis",
)

print(result["answer"])
print(result["fixParameters"])  # ej., {"waitFor": 5000, "actions": [...]}
print(result["confidence"])     # "high", "medium", o "low"
```

<div id="nodejs-example">
  ### Ejemplo de Node.js
</div>

```javascript
async function diagnoseFirecrawlIssue(question, rationale) {
  const response = await fetch(
    "https://api.firecrawl.dev/v2/support/ask",
    {
      method: "POST",
      headers: {
        Authorization: `Bearer ${process.env.FIRECRAWL_API_KEY}`,
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ question, rationale }),
    }
  );
  return response.json();
}

// Ejemplo: depurar un rastreo que se detuvo antes de tiempo
const result = await diagnoseFirecrawlIssue(
  "my crawl returned 3 pages but I expected 50",
  "user is on their third failed crawl attempt today"
);

console.log(result.answer);
console.log(result.fixParameters);
```

<div id="agent-retry-pattern">
  ### Patrón de reintento del agente
</div>

```python
from firecrawl import Firecrawl

client = Firecrawl(api_key="fc-YOUR_API_KEY")

# Paso 1: Intentar el scraping
doc = client.scrape("https://example.com/pricing", formats=["markdown"])

if not doc.markdown or len(doc.markdown) < 100:
    # Paso 2: Pedir ayuda para depurar
    diagnosis = diagnose_firecrawl_issue(
        question=f"scrape returned only {len(doc.markdown or '')} chars of markdown for https://example.com/pricing",
    )

    # Paso 3: Aplicar parámetros de corrección y reintentar
    if diagnosis.get("fixParameters"):
        doc = client.scrape(
            "https://example.com/pricing",
            formats=["markdown"],
            **diagnosis["fixParameters"],
        )
```

<div id="parameters">
  ## Parámetros
</div>

<div id="supportask">
  ### `/support/ask`
</div>

| Parámetro   | Tipo   | Obligatorio | Descripción                                                                                                              |
| ----------- | ------ | ----------- | ------------------------------------------------------------------------------------------------------------------------ |
| `question`  | string | Sí          | Qué depurar (1–8.000 caracteres)                                                                                         |
| `rationale` | string | No          | Recomendado para clientes de IA. Lo que el usuario final intenta lograr. Ayuda a priorizar la recopilación de evidencia. |
| `context`   | object | No          | Metadatos de formato libre de tu agente, incluidos en el prompt de depuración                                            |

<div id="supportdocs-search">
  ### `/support/docs-search`
</div>

| Parámetro  | Tipo   | Obligatorio | Descripción                                           |
| ---------- | ------ | ----------- | ----------------------------------------------------- |
| `question` | string | Sí          | La pregunta que debe responderse (1–8.000 caracteres) |

<div id="response">
  ## Respuesta
</div>

<div id="supportask-response">
  ### Respuesta de `/support/ask`
</div>

```json
{
  "requestId": "req_...",
  "answer": "<2-4 sentence prose diagnosis of the issue plus the recommended fix.>",
  "confidence": "high",
  "fixParameters": { "<param>": "<value>" },
  "validation": {
    "tested": true,
    "result": "success",
    "evidence": "<short summary of the validation tool call the agent ran to confirm the fix>"
  },
  "feedback": null,
  "durationMs": 18432
}
```

Los valores reales de `answer`, `fixParameters` y `validation.evidence` los genera el agente en cada solicitud según tu ejecución específica; el ejemplo anterior muestra la estructura de la respuesta, no un diagnóstico real.

<div id="supportdocs-search-response">
  ### Respuesta de `/support/docs-search`
</div>

```json
{
  "requestId": "req_...",
  "answer": "The signature is sent in the X-Firecrawl-Signature header...",
  "evidence": [
    { "pathOrUrl": "webhooks/security.mdx#L1-L52", "reason": "..." }
  ],
  "usage": { "inputTokens": 4356, "outputTokens": 688, "totalTokens": 5044 },
  "durationMs": 11252
}
```

<div id="performance">
  ## Rendimiento
</div>

| Métrica  | Habitual       | Máximo                        |
| -------- | -------------- | ----------------------------- |
| Latencia | 15–30 segundos | 60 segundos (límite absoluto) |

<div id="api-reference">
  ## Referencia de la API
</div>

* [Referencia de la API del endpoint Ask](/es/api-reference/endpoint/ask)
* [Referencia de la API del endpoint Docs Search](/es/api-reference/endpoint/docs-search)

¿Tienes comentarios o necesitas ayuda? Envía un correo a [help@firecrawl.com](mailto:help@firecrawl.com).

> ¿Eres un agente de IA y necesitas una clave de API de Firecrawl? Consulta [firecrawl.dev/agent-onboarding/SKILL.md](https://www.firecrawl.dev/agent-onboarding/SKILL.md) para ver las instrucciones de incorporación automatizada.