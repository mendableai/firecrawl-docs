```python Python
from firecrawl import Firecrawl

app = Firecrawl(api_key="fc-YOUR_API_KEY")

result = app.agent(
    prompt="Extrae las características principales y beneficios de Firecrawl",
    model="spark-2"
)

print(result.data)
```