```python Python
# pip install firecrawl-py

from firecrawl import FirecrawlApp

app = FirecrawlApp(api_key="YOUR_API_KEY")

# Use auto proxy for automatic retry with stealth on failure
content = app.scrape_url("https://example.com", proxy="auto")
print(content["markdown"])
```
